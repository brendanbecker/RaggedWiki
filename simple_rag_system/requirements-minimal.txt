# Minimal requirements for use with existing venv
# Use this when the venv already has sentence-transformers, torch, numpy, etc.
# (e.g., ~/.claude/skills/sentence-embedding/venv)

# Core RAG dependencies (not in sentence-embedding venv)
chromadb>=0.4.0
tiktoken>=0.5.0

# Markdown processing
markdown>=3.4.0
python-frontmatter>=1.0.0

# CLI and utilities
click>=8.0.0
rich>=13.0.0

# For evaluation (numpy, tqdm already in existing venv)
pandas>=2.0.0
